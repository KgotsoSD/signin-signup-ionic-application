import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-4-app-k',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
